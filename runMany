argCount=$#
echo ${argCount}
if [ "$#" == 2 ]; then
    max=$1
    testname=$2
else
    max=10
    testname=$1
fi

echo Running ${testname} ${max} times

for((i=1;i<=$max;i++))
do
    echo ${testname}
done



